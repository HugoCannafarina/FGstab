% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_stability_paths.R
\name{plot.ipss_result}
\alias{plot.ipss_result}
\title{Plot stability paths from an ipss_result object}
\usage{
\method{plot}{ipss_result}(
  x,
  n_highlight = 5L,
  selected_color = "#E63946",
  alpha_unselected = 0.4,
  ...
)
}
\arguments{
\item{x}{An \code{\link{ipss_result}} object returned by
\code{\link{FGstab}}.}

\item{n_highlight}{Integer. Number of top features to label on the right
margin, ranked by maximum selection frequency. Default \code{5}.}

\item{selected_color}{Character. Colour used for selected feature paths.
Default \code{"#E63946"} (red).}

\item{alpha_unselected}{Numeric in \code{[0, 1]}. Transparency of
unselected feature paths. Default \code{0.4}.}

\item{...}{Additional arguments (ignored).}
}
\value{
A \code{ggplot} object (invisible). The plot is also printed.
}
\description{
Displays the selection frequency of each feature along the regularisation
path. Features that are ultimately selected are highlighted in colour;
all others are drawn in light grey. The top-k most stable features
(by maximum selection frequency) are labelled on the right margin.
}
\examples{
\dontrun{
res <- FGstab(X, y, n_alphas = 15, target_fdr = 0.2)
plot(res)
plot(res, n_highlight = 10)
}

}
